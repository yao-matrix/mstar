# Default deployment configs shipped inside the wheel so `mstar serve <model>`
# works from a pip install. The YAMLs are copied here from the repo-root
# configs/ at build time (see setup.py); in a source checkout this directory
# holds only this marker and the CLI reads configs/ directly.
